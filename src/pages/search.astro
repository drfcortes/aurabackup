---
import Layout from '../layouts/Layout.astro';
---

<Layout>
    <main class="p-8 max-w-4xl mx-auto text-aura-deep dark:text-aura-cream bg-aura-soft dark:bg-aura-deep rounded-2xl shadow">
        <h1 class="text-3xl font-bold text-aura-green dark:text-aura-yellow mb-6">Find an AURA Block</h1>

        <section class="prose prose-lg max-w-none text-left mb-8">
            <p>
                Enter the <strong>AURA Block UID</strong> to view its metadata and traceability record.
                AURA blocks are public records of AI-assisted content generation, including details like the model used,
                prompts, timestamps, and licenses.
            </p>
            <p>
                This search interface does not require an account.
                If you want to register and preserve your own blocks permanently, you can
                <a href="/register" class="text-aura-leather dark:text-aura-yellow hover:text-aura-green dark:hover:text-aura-cream">
                    create an account
                </a>.
            </p>
        </section>

        <section class="bg-aura-beige dark:bg-aura-olive p-6 rounded-xl shadow border border-aura-olive dark:border-aura-gray">
            <h2 class="text-xl font-semibold text-aura-green dark:text-aura-yellow mb-4">Lookup by UID</h2>

            <form id="blockForm" class="space-y-4">
                <label for="uidInput" class="block text-sm font-medium text-aura-gray dark:text-aura-cream">Enter the block UID:</label>
                <input
                        id="uidInput"
                        type="text"
                        placeholder="Example: AURA-2025-000001"
                        class="w-full px-4 py-2 border border-aura-olive dark:border-aura-gray rounded-md bg-white dark:bg-aura-deep text-aura-black dark:text-aura-cream focus:outline-none"
                />
                <button
                        type="submit"
                        class="bg-aura-green dark:bg-aura-leather text-white px-6 py-2 rounded hover:bg-aura-deep dark:hover:bg-aura-yellow transition"
                >
                    View Block
                </button>
            </form>

            <div class="mt-6 text-sm text-aura-gray dark:text-aura-cream">
                Example UID: <code class="bg-gray-100 dark:bg-aura-deep px-2 py-1 rounded">AURA-2025-000001</code>
            </div>
        </section>
    </main>

    <!-- ✅ Script funcional compatible con Astro -->
    <script>
        document.addEventListener("DOMContentLoaded", () => {
            const form = document.getElementById("blockForm");
            const input = document.getElementById("uidInput");

            form.addEventListener("submit", (e) => {
                e.preventDefault();
                const uid = input.value.trim();
                if (uid) {
                    window.location.href = `/block/${uid}`;
                } else {
                    alert("Please enter a UID.");
                }
            });
        });
    </script>
</Layout>
