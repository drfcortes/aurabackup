---
import Layout from '../layouts/Layout.astro';
import TypedMessage from '../components/TypedMessage.jsx';
---

<Layout>
    <main class="p-8 max-w-4xl mx-auto text-center">

        <!-- Ventana simulada -->
        <div class="bg-aura-soft rounded-lg shadow-lg border border-aura-beige overflow-hidden w-full max-w-2xl mx-auto">
            <div class="flex items-center px-4 py-2 bg-aura-beige border-b border-aura-gray/20">
                <div class="flex space-x-2 mr-4">
                    <span class="w-3 h-3 rounded-full bg-red-500"></span>
                    <span class="w-3 h-3 rounded-full bg-yellow-400"></span>
                    <span class="w-3 h-3 rounded-full bg-green-500"></span>
                </div>
                <span class="text-sm text-aura-deep font-semibold">AURA protocol</span>
            </div>

            <!-- Contenido animado -->
            <div class="p-6 text-lg sm:text-xl font-mono text-aura-deep min-h-[120px] flex items-center">
                <TypedMessage client:load />
            </div>
        </div>

        <!-- Texto debajo -->
        <p class="mt-6 text-lg text-aura-deep">
            Transparent and verifiable reporting of AI-assisted content.
        </p>
    </main>
</Layout>
