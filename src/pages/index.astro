---
import Layout from '../layouts/Layout.astro';
import TypedMessage from '../components/TypedMessage.jsx';
---

<Layout>
    <main class="p-8 max-w-4xl mx-auto text-center">

        <!-- Simulación de ventana -->
        <div class="bg-aura-soft rounded-lg shadow-lg border border-aura-beige overflow-hidden w-full max-w-2xl mx-auto">

            <!-- Barra superior de ventana -->
            <div class="flex items-center px-4 py-2 bg-aura-cream border-b border-aura-beige">
                <div class="flex space-x-2">
                    <span class="w-3 h-3 rounded-full bg-red-500"></span>
                    <span class="w-3 h-3 rounded-full bg-yellow-400"></span>
                    <span class="w-3 h-3 rounded-full bg-green-500"></span>
                </div>
                <span class="flex-1 text-center text-sm text-aura-gray font-mono">aura-typing</span>
            </div>

            <!-- Área de animación -->
            <div class="p-6 bg-aura-soft text-left font-mono text-lg text-aura-deep">
                <TypedMessage client:load />
            </div>
        </div>

        <!-- Título debajo -->
        <h1 class="text-4xl font-bold text-aura-green mt-10 mb-6">The AURA Initiative</h1>

        <!-- Texto introductorio -->
        <p class="text-aura-deep mb-4">
            Transparent and verifiable reporting of AI-assisted content.
        </p>
        <p class="text-aura-deep">
            The AURA Protocol (Attribution & Usage Reporting for AI) provides a structured, standardized framework to declare, document, and trace AI-assisted contributions in a verifiable manner.
        </p>
    </main>
</Layout>
